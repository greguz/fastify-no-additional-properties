{
  "name": "fastify-no-additional-properties",
  "version": "2.2.0",
  "description": "Add additionalProperties: false by default to your JSON Schemas",
  "main": "index.js",
  "scripts": {
    "lint": "standard index.js tests",
    "test": "nyc --reporter=lcov --reporter=text-summary tape tests/*.js",
    "prepublishOnly": "npm run lint && npm run test"
  },
  "keywords": [
    "fastify",
    "json",
    "schema",
    "additionalProperties"
  ],
  "author": "Giacomo Gregoletto",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/greguz/fastify-no-additional-properties.git"
  },
  "files": [
    "index.js"
  ],
  "dependencies": {
    "fastify-plugin": "^3.0.1"
  },
  "devDependencies": {
    "fastify": "^3.27.1",
    "fluent-json-schema": "^3.0.1",
    "nyc": "^15.1.0",
    "standard": "^16.0.4",
    "tape": "^5.5.2"
  }
}
